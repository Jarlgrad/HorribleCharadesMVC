<html xmlns="http://www.w3.org/1999/xhtml">
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model HorribleCharadesMVC.Viewmodels.Play.ScoreViewModel
@*@Html.Hidden(Model.game.Code)*@

<head>
    <title>Score</title>
</head>
<body>

    <h2>Team X, @Model.Teams[0].TeamName</h2>
    <h3>GUESSSED</h3>
    <h3>TIME </h3>
    <h3>POINTS</h3>
    <br />
    @*<input type="button" value="Next" onclick="@("window.location.href='" + @Url.Action("Score", "Play", new { id = @Model.Team.PlayerId++ }) + "'");" />*@

    @*<script>

        function loopTeams(id) {
            var url = '@Url.Action("Score", "Play", new { id = "__id__" })';
            window.location.href = url.replace('__id__', id);
        }
    </script>*@

    <script>
    //Scripts taggarna ska flyttas ut till en scriptfil. Kom ihåg att hänvisa till filen när vi flyttar it taggen också.
    var timeLeft = 3;
    var counter = setInterval(timer, 1000);
    function timer()
    {
        timeLeft -= 1;
        if (timeLeft <= 0)
        {
            clearInterval(counter);
            document.getElementById("timer").innerHTML = "Time is up!" //Här ska vi skicka vidare användaren till nästa view
            redirect();
            return;
        }
    }

    function redirect() {
        window.location.href = '/Play/Score/@Model.GameCode';
    }

    </script>

</body>
</html>